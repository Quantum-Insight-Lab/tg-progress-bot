---
number: 6
title: "ci: сверка INV-xx в тестах со спекой (S-7) и запрет магических чисел в domain (S-8)"
state: CLOSED
milestone: "M1 Event Core"
labels: ["layer:ci", "type:chore"]
blocked_by: [2, 4]
url: https://github.com/Quantum-Insight-Lab/tg-progress-bot/issues/6
assignee: plyuschevmax
source: github
---

# ci: сверка INV-xx в тестах со спекой (S-7) и запрет магических чисел в domain (S-8)

## Неопределённость
Не пользовательская: инвариант без теста — долг.

## Инварианты
S-7, S-8. Чекер сканирует `docs/04-invariants.md` и `tests/**/*.ts`. Для старта достаточно `tests/invariants/INV-08.test.ts` из #4.

## События
Нет.

## Сделать
- `scripts/check-invariants.ts`: каждый INV-xx в спеке имеет тест с этим префиксом в имени
- ESLint no-magic-numbers в `src/domain`, исключения 0, 1, -1
- блокирующий режим в CI

## Не делать
- перевод проверки в warning «чтобы пройти»
- требовать тесты до появления `tests/invariants/` так, чтобы #4 нельзя было закрыть: порядок — сначала #4, потом этот чекер

## DoD
- CI падает, если в спеке INV без теста
- числовые пороги в домене без `src/config` ломают линт
- сверка с `docs/09`

Blocked by #2, #4
