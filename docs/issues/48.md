---
number: 48
title: "feat: процесс бота, webhook и циклы"
state: OPEN
milestone: "M7 Go-live"
labels: ["context:infra", "type:feat"]
blocked_by: [47]
url: https://github.com/Quantum-Insight-Lab/tg-progress-bot/issues/48
assignee: plyuschevmax
source: github
---

# feat: процесс бота, webhook и циклы

## Неопределённость
U-2, U-14, U-15 — бот должен принимать webhook и не заводить второй планировщик.

## Инварианты
INV-08 на доставке webhook. Миграции не внутри процесса (`docs/pda/07`).

## События
Нет новых.

## Сделать
- `src/index.ts` поднимает grammY, `startGithubReconcileLoop`, `startDailyReportLoop`
- HTTP: webhook Telegram и GitHub; подпись GitHub уже в ingest
- миграции остаются отдельным шагом

## Не делать
- второй бот, второй Octokit, polling вместо webhook
- мутации GitHub (INV-09)

## DoD
- процесс стартует из env и слушает оба webhook
- циклы — те же функции, не копии
- CI не ослаблен

Blocked by #47
