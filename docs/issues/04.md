---
number: 4
title: "feat: журнал событий emit + UNIQUE idempotency_key, только append"
state: CLOSED
milestone: "M1 Event Core"
labels: ["layer:events", "type:feat"]
blocked_by: [2, 3]
url: https://github.com/Quantum-Insight-Lab/tg-progress-bot/issues/4
assignee: plyuschevmax
source: github
---

# feat: журнал событий emit + UNIQUE idempotency_key, только append

## Неопределённость
U-15 — повтор webhook/кнопки/крона не должен создать второй факт.

## Инварианты
INV-08, INV-16 (тест с этим префиксом в той же задаче). Каркас `tests/invariants/INV-08.test.ts` нужен, чтобы S-7 в #6 мог включиться.

## События
Публикация только через `emit` и сгенерированные константы из `contracts/event-registry.yaml`.

## Сделать
- таблица `events` append-only: нет UPDATE/DELETE на уровне прав БД
- UNIQUE `idempotency_key`
- `emit` из `src/events`
- закоммиченные сгенерированные типы

## Не делать
- запись в `events` в обход `emit`
- строковые литералы типов событий
- проекции

## DoD
- тесты INV-08, INV-16 зелёные
- CI не ослаблен
- сверка с `docs/06-events.md`

Blocked by #2, #3
