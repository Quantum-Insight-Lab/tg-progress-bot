---
number: 2
title: "ci: границы слоёв и контекстов, codegen событий, запрет Date в домене"
state: OPEN
milestone: "M0 Foundation"
labels: ["layer:ci", "type:chore"]
blocked_by: [1]
url: https://github.com/Quantum-Insight-Lab/tg-progress-bot/issues/2
assignee: plyuschevmax
source: github
---

# ci: границы слоёв и контекстов, codegen событий, запрет Date в домене

## Неопределённость
Не пользовательская: защищает S-1, S-2, S-3, S-10.

## Инварианты
Сборочные: S-1, S-2, S-3, S-10 (`docs/09-structural-invariants.md`).

## События
Codegen читает `contracts/event-registry.yaml`; литералы `event_type` в `emit` запрещены.

## Сделать
- `dependency-cruiser`: domain не импортирует telegram/github/projections; `projects` не импортирует `tasks` и наоборот
- `npm run codegen:events` → закоммиченные типы; CI сравнивает повторную генерацию с git
- ESLint: запрет `new Date()` / `Date.now()` в `src/domain`
- Блокирующий CI с первого дня

## Не делать
- мутации GitHub; ослабление проверок «чтобы прошло»
- S-5/S-6/S-9 (это #18)

## DoD
- CI падает на импорте domain→telegram, на `emit("task.checked")`, на `new Date()` в домене
- сверка с `docs/09`

Blocked by #1
