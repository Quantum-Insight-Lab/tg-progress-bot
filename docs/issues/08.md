---
number: 8
title: "feat: контекст tasks — создание, приоритет, исполнитель, переходы статуса"
state: OPEN
milestone: "M2 Domain"
labels: ["context:tasks", "layer:domain", "type:feat"]
blocked_by: [7]
url: https://github.com/Quantum-Insight-Lab/tg-progress-bot/issues/8
assignee: plyuschevmax
source: github
---

# feat: контекст tasks — создание, приоритет, исполнитель, переходы статуса

## Неопределённость
U-1, U-4, U-8, U-9, U-10 — задача с issue, исполнителем и приоритетом.

## Инварианты
INV-01, INV-02, INV-03, INV-15 (тесты с этими префиксами в той же задаче).

## События
`task.created`, `task.prioritized`, `task.reassigned`, `task.postponed`, `task.cancelled`

## Сделать
- создание задачи только с issue и assignee-участником
- вес из приоритета, поле weight не хранить
- таблица переходов из `docs/04-invariants.md` (без REVIEW/DONE/BLOCKED — это #9)
- контекст `projects` не импортировать: связь только событиями

## Не делать
- UI Telegram
- импорт `src/domain/projects`
- правила в будущем хендлере

## DoD
- тесты INV-01, INV-02, INV-03, INV-15 зелёные
- CI не ослаблен
- сверка с ТЗ §2.2

Blocked by #7
