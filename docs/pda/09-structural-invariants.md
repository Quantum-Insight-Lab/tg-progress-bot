> Structural Invariant Sheet (патч 1.2, 9.3 и 9.7). Инвариант сборки защищает единственность и направление механизмов, а не корректность фактов. Колонка «Из ТЗ» у всех — `derived`: инварианты сборки приходят из патча, а не из ТЗ.

# Инварианты сборки

Правило внесения: **инвариант попадает в реестр только вместе с работающей проверкой.** Договорённость без проверки живёт в [`AGENTS.md`](../../AGENTS.md) и заведомо слабее.

Исполнитель — AI-агент. Он не помнит прошлых решений между задачами и копирует ближайший образец из кода, поэтому дрейф механизмов начинается не на втором исполнителе, а на второй задаче.

## Реестр

| ID | Инвариант | Что ломается без него | Механизм | Режим | Из ТЗ |
| --- | --- | --- | --- | --- | --- |
| S-1 | Слои зависят в одну сторону по [07](07-architecture.md): домен не импортирует адаптеры, проекции и инфраструктуру; журнал — ничего, кроме `src/config`; `src/config` — ничего; адаптеры Telegram и GitHub — друг друга; проекции — домен и адаптеры | правила уезжают в хендлеры, домен нельзя проверить отдельно | dependency-cruiser, правила `S-1-*` | CI, блок | derived: патч 1.2, 9.3 |
| S-2 | Контексты `projects`, `tasks`, `github`, `progress` связаны только событиями; общий код — `src/domain/shared` | контексты срастаются, любое изменение задевает всё | dependency-cruiser, `S-2-contexts` | CI, блок | derived: патч 1.2, 9.3 |
| S-3 | Тип события есть в [реестре](../../contracts/event-registry.yaml), типы генерируются, строкового типа в `emit` и `on` нет | два имени одного факта, расхождение схем, проекция подписана на то, что никто не публикует | `npm run codegen:events -- --check`; ESLint `no-restricted-syntax` | CI, блок | derived: патч 1.2, 9.4 |
| S-5 | Проекции не публикуют событий и не пишут журнал | журнал перестаёт быть источником правды (INV-28) | dependency-cruiser: проекции видят только `src/events/generated` | CI, блок | derived: патч 1.2, 9.3 |
| S-6 | Циклов между модулями нет | порядок загрузки не определён, рефакторинг блокируется | dependency-cruiser `no-circular` | CI, блок | derived: патч 1.2, 9.3 |
| S-7 | У каждого INV из [04](04-invariants.md) есть тест, имя которого начинается с его ID | инвариант остаётся текстом в спеке | `npm run check:invariants`; тест с несуществующим ID ломает сборку всегда | CI, отчёт | derived: патч 1.2, 9.3 |
| S-8 | Чисел в домене нет, кроме 0, 1, −1: пороги — из `src/config`, значения совпадают с карточками [05](05-constants.md) | порог вписан в три места с разными значениями, калибровка невозможна | ESLint `no-restricted-syntax` на числовые литералы в `src/domain`; тест `src/config` ↔ 05 | CI, блок | derived: патч 1.2, 9.3 |
| S-10 | Домен не читает системное время, только `Clock`; сутки — по таймзоне проекта (INV-24) | «сегодня» по серверу: канвас, застой и отчёты уезжают на сутки | ESLint на `Date.now()` и `new Date()` в `src/domain`; единственная реализация — `src/infrastructure/clock.ts` | CI, блок | derived: сутки определяют канвас, застой и расписание отчётов |

## Кандидаты

Проверки ещё нет, поэтому в реестр они не входят. Номера зарезервированы, как в каталоге патча.

| Кандидат | Инвариант | Когда вводить |
| --- | --- | --- |
| S-4 | один механизм на задачу по реестру в `AGENTS.md`; тест-перепись реализаций | когда появятся клиент БД, логгер, клиенты Telegram и GitHub |
| S-9 | мёртвый код и неиспользуемые экспорты не копятся (`knip`) | когда кода станет больше каркаса |

## Карточки

### S-3 — единственный источник имён событий

| Поле | Значение |
| --- | --- |
| Механизм | `npm run codegen:events` пишет `src/events/generated/events.ts`: `EVENT_TYPES`, `EVENT_VERSIONS`, конверт, payload каждого события, `PayloadByType`. Файл коммитится, расхождение видно в диффе |
| Где проверяем | CI: `--check` генерирует заново и сравнивает с закоммиченным; ESLint запрещает строку первым аргументом `emit` и `on` |
| Как выглядит нарушение | `emit('task.done', …)` — линтер падает; правка сгенерированного файла руками — падает `--check` |
| Исключения | нет: события, которого нет в реестре, не существует |
| Дальше | схемы валидации payload добавятся в генерацию вместе с журналом событий |

### S-7 — инвариант с тестом

| Поле | Значение |
| --- | --- |
| Механизм | `scripts/check-invariants.ts` сверяет ID из таблицы 04 с именами тестов `it`, `test`, `describe` в `tests/` |
| Режим | отчёт, пока у инвариантов нет кода: сейчас 28 в спеке, 0 с тестом. Тест с ID, которого нет в спеке, ломает сборку уже сейчас. DoD задачи в `AGENTS.md` требует тест на каждый затронутый INV. Когда тесты будут у всех, CI переходит на `--strict` |

### S-10 — явная таймзона

| Поле | Значение |
| --- | --- |
| Механизм | порт `Clock` в `src/domain/shared/clock.ts`; системное время читает только `src/infrastructure/clock.ts` |
| Как выглядит нарушение | `const today = new Date().toISOString().slice(0, 10)` в логике канваса |
| Исключения | нет в `src/domain`; вне домена время берётся тоже через `Clock`, но проверкой это не закреплено |

## Порядок внедрения

До первой фичи сделано: S-1, S-2, S-3, S-5, S-6, S-8, S-10 блокирующе, S-7 отчётом, `AGENTS.md` с реестром механизмов и DoD. S-4 и S-9 — по мере появления механизмов и кода. Метрики слоя «Сборка» — M-13 и M-14 в [08](08-observability.md).
