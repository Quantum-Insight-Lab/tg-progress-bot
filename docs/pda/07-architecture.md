> Blueprint (PDA шаг 6). Архитектура поддерживает граф и инварианты, а не наоборот. Колонка «Из ТЗ» — атомы видов tech и integration, которые решение покрывает, или `derived: причина` для решения, которого в ТЗ нет.

# Архитектура

## Решения

| ID | Решение | Почему | Из ТЗ |
| --- | --- | --- | --- |
| B-1 | Бот в Telegram — единственный интерфейс людей: задачи и канвас в топиках, отчёты в командном топике, админка в личке | задачи живут там, где команда уже общается | R-082 |
| B-2 | Telegram доставляет обновления боту через webhook Bot API | задержка ниже, чем у опроса, и один вход для всех обновлений | R-773 |
| B-3 | Один процесс backend — Progress Engine с частями Tasks, GitHub mirror, Event Processor, Report Generator; брокера и второй единицы развёртывания нет | при этой нагрузке брокер только добавляет отказов; части разделены границами модулей (S-1, S-2), а не сетью | R-774, R-776, R-777, R-778, R-779, R-092, R-093 |
| B-4 | PostgreSQL — единственное хранилище: домен, зеркало и журнал событий | ограничения графа (03) выражаются constraint'ами, журнал и состояние меняются в одной транзакции | R-775 |
| B-5 | GitHub подключается через GitHub App, не через личный токен; webhooks — основной канал, опрос раз в `RECONCILE_INTERVAL` (C-10) догоняет пропуски; зеркало одно на репозиторий | App даёт приватные репозитории, секрет подписи webhooks и свои лимиты | R-068, R-084, R-342, R-343, R-780 |
| B-6 | Канвас — rich message Bot API 10.3: `sendRichMessage` с `message_thread_id` для топика, правка через `editMessageText` с `rich_message` | одно редактируемое сообщение с кнопками внутри текста; сверено с Bot API 10.3 от 24.08.2026 | R-594, R-616 |
| B-7 | Действия — `RichTextButton` с `callback_data` и стилем `link` внутри абзаца задачи; ни клавиатуры под сообщением, ни `tg-button-row` | кнопка стоит в строке задачи и выглядит как ссылка; стиль `link` в Bot API разрешён только callback-кнопкам | R-596 |
| B-8 | Нативные чеклисты не используются: `- [ ]` в rich message — рисунок без callback, `sendChecklist` работает только от имени Business-аккаунта | в топике супергруппы обычному боту чеклист недоступен | R-608, R-610 |
| B-9 | Лимиты rich message — 32768 символов и 500 блоков (C-8, C-9); за пределом канвас ужимается, а потом бот отказывает (A-31) | превышение ломает правку сообщения | R-614, R-615 |
| B-10 | Бот в группе — администратор с правами на сообщения и управление темами | бот создаёт топики «Отчёты» и топики исполнителей | R-539 |
| B-11 | TypeScript и Node 22 | инструменты патча 1.2 — границы слоёв, генерация типов событий, сверка INV — на TypeScript; стек v1 | derived: языка в ТЗ нет |
| B-12 | Telegram-клиент — grammY; методы Bot API 10.3 вызываются напрямую, если их ещё нет в типах библиотеки | зрелая типизированная библиотека с поддержкой топиков; rich messages появились недавно | derived: библиотеки в ТЗ нет |
| B-13 | GitHub-клиент — Octokit, подпись webhooks проверяется секретом App | официальный клиент и проверка подписи из коробки | derived: клиента в ТЗ нет |
| B-14 | SQL — Kysely, без ORM | часть инвариантов — constraint'ы, запросы должны быть явными | derived: слоя доступа к БД в ТЗ нет |
| B-15 | Тесты — Vitest; имя теста на инвариант начинается с его ID | сверка INV-xx со спекой в CI (S-7) | derived: тестов в ТЗ нет |

## Слои и направление зависимостей

Решение архитектуры, в ТЗ его нет; правила границ проверит CI на шаге инвариантов сборки (патч 1.2).

| Слой | Что внутри | Может импортировать |
| --- | --- | --- |
| `src/domain` | законы: переходы статусов, права, доля бэклога, застой, принадлежность фактов (INV) | `src/events` (типы), `src/config` |
| `src/events` | журнал и публикация событий по реестру | `src/config` |
| `src/projections` | канвас, отчёты, срезы, строки — только чтение | `src/events` (типы), чтение БД |
| `src/telegram` | обработчики обновлений, guard доступа, rich messages | `src/domain`, `src/projections`, `src/events` (типы) |
| `src/github` | webhooks, сверка, запись зеркала | `src/domain`, `src/events` (типы) |
| `src/config` | константы C-xx | ничего из `src` |

Контексты `projects`, `tasks`, `github`, `progress` не импортируют друг друга: связь только через события (S-2). Проекции не пишут в журнал (S-5).

## Потоки

1. **Действие человека.** Обновление Telegram → guard доступа (INV-16) → акт домена с проверкой инвариантов → событие в журнал → проекции → правка канваса.
2. **Факт GitHub.** Webhook → проверка подписи → запись зеркала по природному ключу (INV-22) → событие → доля, срезы, факты застоя, строка GitHub.
3. **Время.** Планировщик → акты системы A-28…A-33, A-35, A-36 → события → канвасы и отчёты.

## Проекции и экраны

Проекция только читает: журнал, зеркало и таблицы домена. Она ничего не решает и в журнал не пишет (S-5). Какие события перерисовывают проекцию — поле `projections` в [реестре событий](../../contracts/event-registry.yaml).

| ID | Проекция | Где | Что показывает | Из чего собирается | Из ТЗ |
| --- | --- | --- | --- | --- | --- |
| P-1 | Канвас целиком | топик исполнителя | блоки сверху вниз в порядке §5; одно rich message; кнопки — `RichTextButton` стиля `link` внутри строк, без клавиатуры и ряда кнопок; отвечает на «что сделано, что делается, что мешает, какой следующий шаг» | P-2…P-11 | R-087, R-088, R-089, R-090, R-475, R-597, R-598, R-599 |
| P-2 | Шапка канваса | канвас | имя проекта и дата по таймзоне проекта | `projects`, часы домена | R-107, R-108 |
| P-3 | Доля бэклога | канвас | «Сделано по проекту: N% · осталось X из Y»; без данных — «Нет данных», остаток всегда рядом; у проектов одного репозитория доля одна | функция доли (INV-01, INV-02) | R-100, R-109, R-110, R-175, R-181, R-445 |
| P-4 | Строка человека | канвас | «имя по issues: сделал · сейчас на нём · дальше в репозитории»; без логина — «нет сопоставления с GitHub»; чужой логин не появляется | `issue_assignees`, `users.github_login` (INV-14) | R-111, R-112, R-113, R-114, R-430, R-434 |
| P-5 | Срез issues | канвас | «Сделано», «В работе», «Далее» по `CANVAS_SLICE_SIZE` (C-2); пункт — `#номер название`; в «В работе» имя assignee, незаписанный логин — `@login`, несколько — через запятую; связи issues дописываются к пункту; полный список не вываливается | `issues`, `issue_assignees`, `issue_dependencies` | R-115, R-116, R-117, R-118, R-119, R-120, R-121, R-140, R-151, R-152, R-153, R-383, R-386, R-482, R-484, R-486 |
| P-6 | Блок «Задачи» | канвас | его `IN_PROGRESS`, `BLOCKED`, `REVIEW`; первая строка — кружок-кнопка, номер, название, день, у `REVIEW` — «на подтверждении»; вторая — приоритет, «в план» или «подтвердить» и «вернуть», «отменить»; номеров issue и PR нет; подтверждённая и снятая задача уходит | `tasks`, `canvas_items` (INV-05, INV-09) | R-122, R-123, R-124, R-125, R-126, R-127, R-128, R-129, R-130, R-131, R-132, R-133, R-141, R-143, R-144, R-145, R-218, R-238, R-317, R-327, R-328, R-488, R-489, R-490, R-491, R-492, R-493, R-509, R-648 |
| P-7 | Блок «План» | канвас | его `PLANNED` по приоритету, затем по дате; вторая строка — приоритет, «в работу», «отменить»; кружка нет; пустой план не печатается | `tasks` (INV-08) | R-240, R-494, R-495, R-498, R-580, R-639 |
| P-8 | Блок «Блокеры» | канвас | причины его задач в `BLOCKED`, строка причины — когда есть ответ; его PR без движения, красный CI у них — пометкой; красный CI основной git-ветки — отдельной строкой; зелёный CI и чужой логин строк не дают; число тихих issues не печатается | `blockers`, зеркало (INV-12, INV-13) | R-264, R-268, R-276, R-278, R-375, R-376, R-390, R-399, R-499, R-500, R-501, R-503 |
| P-9 | Строка GitHub | канвас | репозиторий, CI основной git-ветки, открытые PR, коммиты за сутки, ближайший milestone с датой; без даты — без milestone; CI неизвестен — «Нет данных»; без репозитория — «репозиторий не подключён»; отдельного экрана GitHub нет, git-ветки и метки не выводятся, полный список PR не вываливается | `repositories`, `pull_requests`, `commits`, `milestones` | R-085, R-134, R-135, R-136, R-137, R-138, R-182, R-340, R-341, R-357, R-358, R-360, R-363, R-364, R-366, R-381, R-938 |
| P-10 | Строка расхождения | канвас, под строкой GitHub | «В репозитории есть движение, в задачах за сутки тишина» | функция сигнала (INV-15) | R-414, R-508 |
| P-11 | Строка динамики | канвас | `DYNAMICS_POINTS` точек с шагом `DYNAMICS_STEP` (C-5, C-6), пропуски не заполняются | `progress_snapshots` (INV-02) | R-506, R-507, R-582 |
| P-12 | Ежедневный отчёт целиком | личка, командный топик | «За сутки · дата»; строка «Все проекты: было → стало · осталось»; блок на каждый проект и блок на каждый репозиторий; в личке — проекты человека и их репозитории, в командном топике — все проекты группы | P-13, P-14, функция доли (INV-25, INV-26) | R-171, R-463, R-464, R-654, R-658, R-663, R-664, R-665, R-666, R-667, R-756 |
| P-13 | Блок проекта в отчёте | отчёт | «Бэклог: было → стало · осталось», «Закрыто», «Открыто новых», «Задачи: подтверждено · создано · отменено · встало в блок», «Сейчас» с именем в командном топике и днём, «Дальше», «Риск», строка расхождения; пустые строки не печатаются, длинные списки — числом и до `REPORT_LIST_LIMIT` названий (C-3) | журнал за период, зеркало (INV-25) | R-279, R-511, R-668, R-669, R-670, R-671, R-672, R-673, R-674, R-675, R-676, R-677, R-678, R-679, R-680, R-681, R-682, R-683, R-689, R-737, R-749, R-751, R-752 |
| P-14 | Блок GitHub в отчёте | отчёт | репозиторий, CI, число смерженных PR, число коммитов, имена проектов этого репозитория; один на репозиторий, без названий issues и без полного списка коммитов | зеркало за период (INV-26) | R-377, R-378, R-379, R-380, R-382, R-684, R-693 |
| P-15 | Вопрос о блокере | топик исполнителя | «N не двигается K-й день, что мешает?» с кнопкой «нет блокера» | `blockers`, `tasks` | R-255 |
| P-16 | Напоминание руководителям | топик исполнителя | напоминание о задаче на подтверждении с упоминанием руководителей проекта | `tasks`, `project_members` | R-330 |
| P-17 | Первое сообщение сотруднику | личка и топик исполнителя | где его топик и канвас; задача — `/task`; галочка отправляет задачу руководителю и процент не меняет; место в бэклоге — issues, где он assignee; без логина этой строки нет | `project_members`, `users` | R-574, R-575, R-576, R-577, R-578 |
| P-18 | Люди без проекта | личка корня | кто уже нажал `/start` и ещё не в проекте | `users`, `project_members` | R-590 |
