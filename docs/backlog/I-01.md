---
id: I-01
title: "events — журнал событий: `emit`, конверт, идемпотентность, схемы из реестра"
milestone: M1 Ядро
labels: [context:events]
blocked_by: []
state: open
---

# I-01. events — журнал событий: `emit`, конверт, идемпотентность, схемы из реестра

## Зачем

- U-18 — Не применился ли факт GitHub дважды и не потерялся ли?

## PDA

INV-22, INV-28 · E-17 · B-4, B-14 · S-3

## Атомы ТЗ

- [ ] R-775 — PostgreSQL · §8. Архитектура
- [ ] R-929 — `events`: `id`
- [ ] R-930 — `events`: `source`
- [ ] R-931 — `events`: `event_type`
- [ ] R-932 — `events`: `payload`
- [ ] R-933 — `events`: `created_at`
- [ ] R-934 — `events`: Журнал только дополняется

## Сделать

- уникальный ключ идемпотентности в журнале: факт с тем же ключом второй раз не применяется (INV-22) — механизм для всех событий реестра;
- `emit` — единственная запись в журнал, журнал только дополняется (INV-28);
- схемы Zod генерируются из реестра вместе с типами (S-3, карточка в [09](../pda/09-structural-invariants.md)); зависимость `zod` — отдельным коммитом с обоснованием.

## DoD

- все атомы отмечены; неотмеченный — новая issue или `clarify`, не «почти готово»;
- тесты INV-22, INV-28 зелёные, имя теста начинается с ID инварианта (S-7);
- поля — миграцией, ограничения — constraint'ами;
- `npm run ci` зелёный, ни одна проверка не ослаблена; PR перечисляет закрытые атомы.
