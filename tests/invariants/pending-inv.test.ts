import { it } from "vitest";

/** Имена для S-7 до появления домена. Реальные проверки — #7, #8, #9, #16. */
it.todo("INV-03: переходы только по таблице переходов");
it.todo("INV-04: DONE только по акту lead");
it.todo("INV-05: задача не может быть DONE при активном блокере");
it.todo("INV-10: прогресс считается только по задачам своего проекта");
it.todo("INV-11: пустой знаменатель даёт null, не процент");
it.todo("INV-12: доступ к данным проекта только участнику");
it.todo("INV-13: BLOCKED ровно при активном блокере, снятие → IN_PROGRESS");
