# Реестр атомов ТЗ (PDA 1.3, 10.6). Текст атома живёт только в ТЗ, здесь — вид, релиз и решения.
# Проверяется `npm run tz:check`. Новый атом получает следующий свободный номер; номера не переиспользуются.
source: docs/tz.md

glossary:
  ветка: топик исполнителя в супергруппе проекта; «ветка исполнителя» и «топик исполнителя» — одно и то же
  топик: topic Telegram внутри супергруппы — топик исполнителя или командный топик
  линейка: прогресс проекта — доля completed issues репозитория и остаток открытых; «процент проекта» и «доля бэклога» — то же самое
  задача: шаг, который человек ведёт в боте; issue задачей не является и задачу не порождает

atoms:
  # §12 Объём работ · MVP
  R-001: { kind: scope, release: mvp }
  R-002: { kind: scope, release: mvp }
  R-003: { kind: scope, release: mvp }
  R-004: { kind: scope, release: mvp }
  R-005: { kind: scope, release: mvp }
  R-006: { kind: scope, release: mvp }
  R-007: { kind: scope, release: mvp }
  R-008: { kind: scope, release: mvp }
  R-009: { kind: scope, release: mvp }
  R-010: { kind: scope, release: mvp }
  R-011: { kind: scope, release: mvp }
  R-012: { kind: scope, release: mvp }
  R-013: { kind: scope, release: mvp }
  R-014: { kind: scope, release: mvp }
  R-015: { kind: scope, release: mvp }
  R-016: { kind: scope, release: mvp }
  R-017: { kind: scope, release: mvp }
  R-018: { kind: scope, release: mvp }
  R-019: { kind: scope, release: mvp }
  R-020: { kind: scope, release: mvp }
  R-021: { kind: scope, release: mvp }
  R-022: { kind: scope, release: mvp }
  R-023: { kind: scope, release: mvp }
  R-024: { kind: scope, release: mvp }
  R-025: { kind: scope, release: mvp }
  R-026: { kind: scope, release: mvp }
  R-027: { kind: scope, release: mvp }
  R-028: { kind: scope, release: mvp }
  R-029: { kind: scope, release: mvp }
  R-030: { kind: scope, release: mvp }
  R-031: { kind: scope, release: mvp }
  R-032: { kind: scope, release: mvp }
  R-033: { kind: scope, release: mvp }
  R-034: { kind: scope, release: mvp }
  R-035: { kind: scope, release: mvp }
  R-036: { kind: scope, release: mvp }
  R-037: { kind: scope, release: mvp }
  R-038: { kind: scope, release: mvp }
  R-039: { kind: scope, release: mvp }
  R-040: { kind: scope, release: mvp }
  R-041: { kind: scope, release: mvp }
  R-042: { kind: scope, release: mvp }
  R-043: { kind: scope, release: mvp }
  R-044: { kind: scope, release: mvp }
  R-045: { kind: scope, release: mvp }

  # §12 Объём работ · Не входит в MVP. release: later — если пункт назван и в «Дальше».
  R-046: { kind: scope, release: out }
  R-047: { kind: scope, release: out }
  R-048: { kind: scope, release: out }
  R-049: { kind: scope, release: out }
  R-050: { kind: scope, release: later }
  R-051: { kind: scope, release: out }
  R-052: { kind: scope, release: out }
  R-053: { kind: scope, release: out }
  R-054:
    kind: scope
    release: out
    note: из AI в «Дальше» назван только пересказ блоков отчёта — R-057
  R-055: { kind: scope, release: later }
  R-056: { kind: scope, release: later }

  # §12 Объём работ · Дальше
  R-057: { kind: scope, release: later }
  R-058: { kind: scope, release: later, decision: same_as, same_as: R-055 }
  R-059: { kind: scope, release: later }
  R-060: { kind: scope, release: later, decision: same_as, same_as: R-050 }
  R-061: { kind: scope, release: later }
  R-062: { kind: scope, release: later }
  R-063: { kind: scope, release: later, decision: same_as, same_as: R-056 }
  R-064: { kind: scope, release: later }
  R-065: { kind: scope, release: later }
  R-066: { kind: scope, release: later }

  # Преамбула
  R-067: { kind: entity, scope: R-009 }
  R-068: { kind: integration, scope: R-043 }
  R-069: { kind: integration, scope: R-043 }
  R-070: { kind: integration, scope: R-043 }
  R-071: { kind: integration, scope: R-043 }
  R-072: { kind: integration, scope: R-043 }
  R-073: { kind: integration, scope: R-043 }
  R-074: { kind: rule, scope: R-011 }
  R-075: { kind: entity, scope: R-008 }
  R-076: { kind: entity, scope: R-038 }
  R-077: { kind: rule, scope: R-011 }
  R-078: { kind: rule, scope: R-027 }
  R-079: { kind: rule, scope: R-012 }
  R-080: { kind: rule, scope: R-013 }
  R-081: { kind: rule, scope: R-014 }

  # Цель
  R-082: { kind: tech, scope: R-001 }
  R-083: { kind: rule, scope: R-001 }
  R-084: { kind: integration, scope: R-043 }
  R-085: { kind: view, scope: R-021 }
  R-086: { kind: rule, scope: R-009 }
  R-087: { kind: view, scope: R-017 }
  R-088: { kind: view, scope: R-018 }
  R-089: { kind: view, scope: R-020 }
  R-090: { kind: view, scope: R-019 }
  R-091: { kind: tech, scope: R-001, decision: same_as, same_as: R-082 }
  R-092: { kind: tech, scope: R-012 }
  R-093: { kind: tech, scope: R-015 }
  R-094: { kind: integration, scope: R-012 }
  R-095: { kind: integration, scope: R-043, decision: same_as, same_as: R-069 }
  R-096: { kind: integration, scope: R-043, decision: same_as, same_as: R-070 }
  R-097: { kind: integration, scope: R-043, decision: same_as, same_as: R-071 }
  R-098: { kind: integration, scope: R-043, decision: same_as, same_as: R-072 }
  R-099: { kind: rule, scope: R-012 }
  R-100: { kind: view, scope: R-016 }
  R-101: { kind: rule, scope: R-012 }

  # §1 Основной сценарий
  R-102: { kind: entity, scope: R-042 }
  R-103:
    kind: rule
    scope: R-001
    decision: clarify
    defect: D-5
    note: >-
      «Там одно сообщение бота — канвас», но в ту же ветку бот пишет вопрос о блокере (§2.3)
      и первое сообщение сотруднику (§5.2), а канвас каждый день новый (§6). «Одно» — это канвас на дату?
  R-104: { kind: rule, scope: R-004 }
  R-105: { kind: act, scope: R-027 }
  R-106: { kind: reaction, scope: R-028 }
  # §1 · макет канваса: поле — атом, значения — иллюстрация (10.5, правило 4)
  R-107: { kind: view, scope: R-001 }
  R-108: { kind: view, scope: R-001 }
  R-109: { kind: view, scope: R-016 }
  R-110: { kind: view, scope: R-013 }
  R-111: { kind: view, scope: R-015 }
  R-112:
    kind: view
    scope: R-015
    decision: clarify
    defect: D-5
    note: >-
      «закрыл 4»: по §3.4 это issues completed, где человек assignee («Прошёл»),
      а «кто закрыл» в §3.2 — это closed_by. Что считает строка?
  R-113: { kind: view, scope: R-015 }
  R-114: { kind: view, scope: R-015 }
  R-115: { kind: view, scope: R-017 }
  R-116: { kind: view, scope: R-017 }
  R-117: { kind: view, scope: R-017 }
  R-118: { kind: view, scope: R-017 }
  R-119:
    kind: view
    scope: R-017
    decision: clarify
    defect: D-2
    note: >-
      Имя у issue в «В работе» — пользователь бота по github_login assignee. Что печатать,
      если логин ни на кого не записан, и как печатать несколько assignees?
  R-120: { kind: view, scope: R-017 }
  R-121: { kind: view, scope: R-017 }
  R-122: { kind: view, scope: R-018 }
  R-123:
    kind: view
    scope: R-018
    decision: clarify
    defect: D-5
    note: >-
      Чем исполнитель ставит и снимает галочку? Во второй строке IN_PROGRESS такой кнопки нет,
      а `- [ ]` по §6 — рисунок без callback. Кружок у названия — это кнопка (RichTextButton)?
  R-124: { kind: view, scope: R-018 }
  R-125: { kind: view, scope: R-018 }
  R-126: { kind: view, scope: R-029 }
  R-127: { kind: view, scope: R-010 }
  R-128: { kind: view, scope: R-019 }
  R-129: { kind: view, scope: R-018 }
  R-130: { kind: view, scope: R-018 }
  R-131: { kind: view, scope: R-018 }
  R-132: { kind: view, scope: R-018 }
  R-133: { kind: view, scope: R-018 }
  R-134: { kind: view, scope: R-021 }
  R-135: { kind: view, scope: R-021 }
  R-136: { kind: view, scope: R-021 }
  R-137: { kind: view, scope: R-021 }
  R-138: { kind: view, scope: R-021 }
  # §1 · после макета
  R-139: { kind: rule, scope: R-017 }
  R-140: { kind: view, scope: R-017 }
  R-141: { kind: view, scope: R-018 }
  R-142: { kind: view, scope: R-018, decision: same_as, same_as: R-123 }
  R-143: { kind: view, scope: R-010 }
  R-144: { kind: view, scope: R-018 }
  R-145: { kind: view, scope: R-018 }
  R-146: { kind: rule, scope: R-014 }
  R-147: { kind: rule, scope: R-014 }
  R-148: { kind: rule, scope: R-012 }
  R-149: { kind: rule, scope: R-012 }
